{% set name = "minipcn" %}
{% set version = "0.2.0" %}
{% set python_min = "3.10" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/minipcn-{{ version }}.tar.gz
  sha256: 70c70062e748b5883084739433b27f59eb159584e4cb3a51b296bb26c7dd0e4b

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools >=45
    - setuptools-scm >=6.2
    - pip
  run:
    - python >={{ python_min }}
    - matplotlib-base
    - numpy
    - scipy
    - tqdm
    - array-api-compat
    - orng

test:
  imports:
    - minipcn
  commands:
    - pip check
    - python -m pytest tests/
  requires:
    - python {{ python_min }}
    - pip
    - pytest
  source_files:
    - tests
    - pyproject.toml

about:
  summary: A minimal preconditioned Crank-Nicolson MCMC sampler
  home: https://github.com/mj-will/minipcn
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - mj-will
